extends ../app/layout.pug

block content
  h1 Nuevo canto
  form(action="/cantos", method="POST")
    .form-group
      label(for="nombre") Nombre
      input#nombre.form-control(name="nombre")
    .form-group
      label(for="contenido") Contenido
      textarea#contenido.form-control(name="contenido")
    .text-center
      .btn-group
        button.btn.btn-primary.btn-lg(type="submit") Añadir
        a.btn.btn-primary.btn-lg(href="/cantos/agregar") Volver
